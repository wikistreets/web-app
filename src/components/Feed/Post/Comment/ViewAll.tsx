import {
  Popover,
  PopoverContent,
  PopoverTrigger,
} from "@/components/ui/popover";

type Props = {};

const ViewAll = (props: Props) => {
  return <div className="w-full">View all comments</div>;
};

export default ViewAll;
